# YieldSwarm npm defaults
# On Termux/Android, use scripts/termux/proot-bootstrap.sh — do not npm install on raw Bionic.

optional=true
fund=false
audit=false

# If a fork pulls @matrix-org/matrix-sdk-crypto-nodejs, allow install to continue when skipped:
# export MATRIX_SDK_CRYPTO_NODEJS_SKIP_INSTALL=1
