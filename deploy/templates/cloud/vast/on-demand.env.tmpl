# Rendered: deploy/rendered/cloud/vast/on-demand.env
# Vast.ai GPU on-demand instance — env-driven, no baked credentials.

VAST_API_KEY=${VAST_API_KEY}
VAST_IMAGE=${ODYSSEUS_IMAGE}
VAST_GPU_MODEL=${VAST_GPU_MODEL:-RTX_4090}
VAST_DISK_GB=${VAST_DISK_GB:-100}
VAST_ONSTART=cd /app && ./deploy/entrypoint.monitor.sh &
ODYSSEUS_BRAIN_URL=${ODYSSEUS_BRAIN_URL:-http://127.0.0.1:8090}
LLM_ROUTER_PORT=${LLM_ROUTER_PORT}
YIELDSWARM_ROUTER_API_KEY=${YIELDSWARM_ROUTER_API_KEY}
METRICS_URL=${METRICS_URL}
TEMP_THRESHOLD_CELSIUS=${TEMP_THRESHOLD_CELSIUS}
