# Rendered: deploy/rendered/cloud/aws/ecs-task.env
# AWS ECS Fargate task environment — no hardcoded secrets.

AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION}
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
ECS_CLUSTER=${ECS_CLUSTER:-yieldswarm}
ECS_TASK_IMAGE=${BACKEND_IMAGE}
ECS_CONTAINER_PORT=8080
VAULT_ADDR=${VAULT_ADDR}
VAULT_KV_MOUNT=${VAULT_KV_MOUNT}
NEON_DATABASE_URL=${NEON_DATABASE_URL}
SENTRY_DSN=${SENTRY_DSN}
GREEK_LAYER__STRICT_SANITIZATION=${GREEK_LAYER__STRICT_SANITIZATION}
