{
  "buildCommand": "cd kairo/app && npm install && npm run build",
  "outputDirectory": "kairo/app/dist",
  "framework": null,
  "rewrites": [{ "source": "/api/(.*)", "destination": "https://api.yieldswarm.crypto/api/$1" }]
}
