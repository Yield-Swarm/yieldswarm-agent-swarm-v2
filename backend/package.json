{
  "name": "yieldswarm-integration-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Integration layer that feeds the Arena dashboard with live Akash worker + on-chain telemetry (emission router, treasury splits, agent leaderboard).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test src/**/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@solana/web3.js": "^1.95.3",
    "axios": "^1.7.9",
    "ethers": "^6.13.4",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
