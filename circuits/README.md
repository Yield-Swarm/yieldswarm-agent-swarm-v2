# Entropy proof circuit — Paradigm Shift ($PDs^1$)
#
# Build (requires circom + circomlib):
#   cd circuits && npm install && npx circom entropy_proof.circom --r1cs --wasm --sym
#
# Proves telemetry readings are within policy without exposing raw metrics.
