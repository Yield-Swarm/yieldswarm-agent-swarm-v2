.terraform/
# .terraform.lock.hcl IS committed -- pins provider versions for
# reproducible builds across CI and operators.
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
